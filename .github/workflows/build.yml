name: Build QMK and Vial Firmware
on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  KEYBOARD_NAME: "tomtho"
  USER_REPO_PATH: "keyboards/tomtho"  # ← ここを修正！

jobs:
  # ======================================
  # 🔹 Vial QMK版ファームウェアのビルド
  # ======================================
  build_vial:
    name: Build Vial Firmware
    runs-on: ubuntu-latest
    container: qmkfm/qmk_cli:latest

    steps:
      - name: Checkout Vial QMK
        uses: actions/checkout@v4
        with:
          repository: vial-kb/vial-qmk
          path: /__w/qmk-tomtho/qmk-tomtho/vial-qmk

      - name: Checkout user repo
        uses: actions/checkout@v4
        with:
          path: /__w/qmk-tomtho/qmk-tomtho/user_repo

      - name: Copy custom keyboard (instead of symlink)
        run: |
          mkdir -p /__w/qmk-tomtho/qmk-tomtho/vial-qmk/keyboards/${{ env.KEYBOARD_NAME }}
          cp -r /__w/qmk-tomtho/qmk-tomtho/user_repo/${{ env.USER_REPO_PATH }}/* \
                /__w/qmk-tomtho/qmk-tomtho/vial-qmk/keyboards/${{ env.KEYBOARD_NAME }}/
          echo "✅ Copied custom keyboard files."

      - name: Build Vial firmware
        working-directory: /__w/qmk-tomtho/qmk-tomtho/vial-qmk
        run: |
          make ${{ env.KEYBOARD_NAME }}:vial
          echo "=== Build Complete ==="
          find .build/ -type f \( -name "*.uf2" -o -name "*.hex" -o -name "*.bin" \) -ls || true

      - name: Collect artifacts
        run: |
          mkdir -p /__w/qmk-tomtho/qmk-tomtho/artifacts
          cp .build/${{ env.KEYBOARD_NAME }}_vial.* /__w/qmk-tomtho/qmk-tomtho/artifacts/ || true

      - name: Upload Vial firmware artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.KEYBOARD_NAME }}-vial-firmware
          path: /__w/qmk-tomtho/qmk-tomtho/artifacts/

  # ======================================
  # 🔹 通常QMK版ファームウェアのビルド
  # ======================================
  build_qmk:
    name: Build QMK Firmware
    runs-on: ubuntu-latest
    container: qmkfm/qmk_cli:latest

    steps:
      - name: Checkout QMK
        uses: actions/checkout@v4
        with:
          repository: qmk/qmk_firmware
          path: /__w/qmk-tomtho/qmk-tomtho/qmk

      - name: Checkout user repo
        uses: actions/checkout@v4
        with:
          path: /__w/qmk-tomtho/qmk-tomtho/user_repo

      - name: Copy custom keyboard (instead of symlink)
        run: |
          mkdir -p /__w/qmk-tomtho/qmk-tomtho/qmk/keyboards/${{ env.KEYBOARD_NAME }}
          cp -r /__w/qmk-tomtho/qmk-tomtho/user_repo/${{ env.USER_REPO_PATH }}/* \
                /__w/qmk-tomtho/qmk-tomtho/qmk/keyboards/${{ env.KEYBOARD_NAME }}/
          echo "✅ Copied custom keyboard files."

      - name: Build QMK firmware
        working-directory: /__w/qmk-tomtho/qmk-tomtho/qmk
        run: |
          make ${{ env.KEYBOARD_NAME }}:default
          echo "=== Build Complete ==="
          find .build/ -type f \( -name "*.uf2" -o -name "*.hex" -o -name "*.bin" \) -ls || true

      - name: Collect artifacts
        run: |
          mkdir -p /__w/qmk-tomtho/qmk-tomtho/artifacts
          cp .build/${{ env.KEYBOARD_NAME }}_default.* /__w/qmk-tomtho/qmk-tomtho/artifacts/ || true

      - name: Upload QMK firmware artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.KEYBOARD_NAME }}-qmk-firmware
          path: /__w/qmk-tomtho/qmk-tomtho/artifacts/
